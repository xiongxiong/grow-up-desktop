import styled, { css } from "styled-components";
import BasicBtn from "../BasicBtn";

const ToolBtn = styled(BasicBtn)`
    width: 32px;
    height: 32px;
    border: 1px dashed lightgray;
    border-radius: 4px;
`;

export { ToolBtn as default };
